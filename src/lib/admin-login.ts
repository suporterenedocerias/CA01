/** E-mail padrão da conta admin no Supabase Auth. */
export const ADMIN_EMAIL_DOMAIN = 'entulhohoje.com';

/** Conta admin padrão (Supabase Auth usa este e-mail) */
export const DEFAULT_ADMIN_EMAIL = `admin@${ADMIN_EMAIL_DOMAIN}`;

/** Pré-preenchimento e login automático só em `npm run dev` — a conta no Supabase deve usar esta senha. */
export const DEFAULT_DEV_ADMIN_PASSWORD = '@Padrao123';
