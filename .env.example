# Copie para .env na raiz e preencha (Supabase → Settings → API)
# Chave "anon" / public — segura para o browser
VITE_SUPABASE_URL=https://SEU_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sua_chave_anon_public_aqui

# Opcional: API no mesmo domínio (Nginx /api → Node)
# VITE_API_URL=/api
